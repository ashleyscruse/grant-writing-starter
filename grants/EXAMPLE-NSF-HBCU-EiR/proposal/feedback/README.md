# Feedback

Your notes on each rendered version, in markdown, named to match: `v1-feedback.md` for `versions/v1.pdf`, and so on. Plain language is fine. The AI reads the matching feedback file when it revises.
